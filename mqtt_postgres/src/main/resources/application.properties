# Server
server.port=8080

# MQTT Configuration
mqtt.broker=47.120.68.200
mqtt.port=1883
mqtt.topic=mga/pub
mqtt.control-topic=mga/sub
mqtt.client-id=consumer-client-2
mqtt.username=kircheis
mqtt.password=panning

# Spring DataSource (enables JdbcTemplate + HikariCP connection pool)
spring.datasource.url=jdbc:postgresql://localhost:5432/sensordb
spring.datasource.username=postgres
spring.datasource.password=123456
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=300000

# thread
thread.count=1

# Actuator Health Check
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
